import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.eduardozamorat2ph',
  appName: 'eduardozamorat2ph',
  webDir: 'www'
};

export default config;
